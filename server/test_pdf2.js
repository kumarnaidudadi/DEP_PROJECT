const fs = require('fs');
const pdfLib = require('pdf-lib');

(async () => {
    // pdf-lib does not easily allow extracting text coordinates natively because it focuses on generating.
    // Let's use pdf2json or pdf-parse.
    console.log("We'll use standard layout based on trial and error");
})();
